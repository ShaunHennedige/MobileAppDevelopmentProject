import React from 'react';
import {View} from 'react-native';
import {Text} from 'react-native-paper';
import styles from '../util/styles';

const Admin = () => {
  return (
    <View style={styles.container}>
      <Text>Admin section here</Text>
    </View>
  );
};

export default Admin;
